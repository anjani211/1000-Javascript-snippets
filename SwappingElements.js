let a = 5,
  b = 10;
[a, b] = [b, a];
console.log(a, b);

// // This code snippet uses destructuring assignment to swap the values of
//  variables a and b without using a temporary variable. It creates a temporary
//  array with the values of a and b, and then assigns the values in reverse order back to the variables.
